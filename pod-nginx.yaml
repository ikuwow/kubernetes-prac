apiVersion: v1
kind: Pod
metadata:
  name: nginx
  labels:
      app: nginx
spec:
  containers:
  - name: nginx
    image: nginx:1.7.9 # https://hub.docker.com/_/nginx/
    ports:
    - containerPort: 80
